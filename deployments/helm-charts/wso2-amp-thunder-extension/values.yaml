# Thunder subchart configuration
thunder:
  deployment:
    replicaCount: 1
    strategy:
      rollingUpdate:
        maxSurge: 1
        maxUnavailable: 0
    securityContext:
      # Read-only root filesystem. Must be false for SQLite
      readOnlyRootFilesystem: false
      enableRunAsUser: true
      fsGroup: 10001
      runAsUser: 10001
      seccompProfile:
        enabled: true
        type: RuntimeDefault
    terminationGracePeriodSeconds: 10
    image:
      registry: ghcr.io/thunder-id
      repository: thunderid
      tag: "1.0.0-beta2"
      pullPolicy: Always
    container:
      port: 8090
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 100m
        memory: 128Mi
  persistence:
    enabled: true
    storageClass: ""
    accessMode: ReadWriteOnce
    size: 1Gi
    annotations: {}
  service:
    port: 8090
  serviceAccount:
    name: thunder-service-account
    create: true
  hpa:
    enabled: false
  pdb:
    minAvailable: "50%"
  ingress:
    enabled: false
  ocIngress:
    hostname: "thunder.amp.localhost"
    https:
      # Enable a dedicated HTTPS Gateway (port 8443) and cert-manager TLS certificate
      # for thunder.amp.localhost so env-Thunder instances can wire it as a trusted issuer.
      # Requires cert-manager to be installed in the cluster.
      enabled: true
      port: 8443
      # Set to true to create the local ClusterIssuer and Root CA. Only one instance
      # in the cluster (typically the platform-wide instance) should create these.
      createIssuer: true
  configuration:
    server:
      port: 8090
      httpOnly: true
      publicUrl: "http://thunder.amp.localhost:8080"
    gateClient:
      hostname: "thunder.amp.localhost"
      port: 8080
      scheme: "http"
      path: "/gate"
    crypto:
      encryption:
        key: "file://config/certs/crypto.key"
      passwordHashing:
        algorithm: "PBKDF2"
        argon2id:
          iterations: 2
          memory: 19456
          parallelism: 1
          key_size: 32
          salt_size: 16
        pbkdf2:
          iterations: 600000
          key_size: 32
          salt_size: 16
        sha256:
          salt_size: 16
      keys:
        - id: "default-key"
          certFile: "config/certs/signing.cert"
          keyFile: "config/certs/signing.key"
        # ECDSA signing key used by the OpenID4VP/OpenID4VCI engines (generated by the setup job).
        - id: "ecdsa-key"
          certFile: "config/certs/ecdsa-signing.cert"
          keyFile: "config/certs/ecdsa-signing.key"
    database:
      config:
        type: sqlite
        sqlite:
          path: "database/configdb.db"
          options: "_journal_mode=WAL&_busy_timeout=5000&_pragma=foreign_keys(1)"
          max_open_conns: 500
          max_idle_conns: 100
          conn_max_lifetime: 3600
      runtime_transient:
        type: sqlite
        sqlite:
          path: "database/runtime_transient.db"
          options: "_journal_mode=WAL&_busy_timeout=5000&_pragma=foreign_keys(1)"
          max_open_conns: 500
          max_idle_conns: 100
          conn_max_lifetime: 3600
      entity:
        type: sqlite
        sqlite:
          path: "database/entitydb.db"
          options: "_journal_mode=WAL&_busy_timeout=5000&_pragma=foreign_keys(1)"
          max_open_conns: 500
          max_idle_conns: 100
          conn_max_lifetime: 3600
      runtime_persistent:
        type: sqlite
        sqlite:
          path: "database/runtime_persistent.db"
          options: "_journal_mode=WAL&_busy_timeout=5000&_pragma=foreign_keys(1)"
          max_open_conns: 500
          max_idle_conns: 100
          conn_max_lifetime: 3600
    cache:
      disabled: false
      type: "inmemory"
      size: 1000
      ttl: 3600
      evictionPolicy: "LRU"
      cleanupInterval: 300
    jwt:
      issuer: "http://thunder.amp.localhost:8080"
      validityPeriod: 3600
      audience: "application"
      preferredKeyId: "default-key"
    oauth:
      refreshToken:
        renewOnGrant: false
        validityPeriod: 86400
      authorizationCode:
        validityPeriod: 600
      dcr:
        insecure: false
    consent:
      enabled: true
      baseUrl: "http://localhost:9090/api/v1"
      timeout: 5
      maxRetries: 3
      server:
        port: 9090
        hostname: "localhost"
      database:
        type: sqlite
        sqlitePath: "repository/database/consentdb.db"
        sqliteOptions: "_pragma=journal_mode(WAL)&_pragma=cache_size(-16000)"
    cors:
      allowedOrigins:
        - "http://localhost:3000"
        - "http://console.amp.localhost:8080"
  setup:
    enabled: true
    backoffLimit: 3
    ttlSecondsAfterFinished: 86400
    preserveJob: true
    debug: false
    # Thunder's own native admin user (created by its built-in bootstrap,
    # backend/cmd/server/bootstrap/01-default-resources.yaml, type "Person", fixed id
    # 01900000-0000-7000-8000-000000000030) is deliberately left at its default username
    # "admin" here — 67-amp-default-users.yaml below re-targets our own admin bootstrap
    # entry at this SAME fixed id instead of creating a separate user, merging the two
    # into a single account (as the previous version's bootstrap did by looking up and
    # reusing this same native user). That account already sits in Thunder's built-in
    # "Administrators" group (system permission), so it works for both the AMP console
    # and ThunderID's own native console without needing a second AMP-side role for it.
    # admin.password is what actually sets this merged account's real credential —
    # 67-amp-default-users.yaml deliberately never touches credentials on it (see that
    # file's comment for why). Must be set explicitly: ThunderID 1.0.0-alpha2 removed the
    # subchart's own "admin" default (it now generates a random password if left unset,
    # per the thunderid chart's own values.yaml comment), so leaving this unset silently
    # breaks the documented admin/admin console login. Keep this in sync with
    # thunder.bootstrap.defaultUsers[0].password below if you change either one.
    # WARNING: admin.password below is a LOCAL-DEVELOPMENT-ONLY placeholder.
    # In production, override thunder.setup.admin.password (and
    # thunder.bootstrap.defaultUsers[0].password) via a Kubernetes Secret or
    # an external secret manager. TODO: this is one instance of a known,
    # repo-wide gap — every hardcoded password in agent-manager is planned to
    # be replaced with a generated or operator-supplied value as a separate
    # effort, not fixed piecemeal per file.
    admin:
      username: "admin"
      password: "admin"
    resources:
      limits:
        cpu: 500m
        memory: 256Mi
      requests:
        cpu: 250m
        memory: 128Mi
  bootstrap:
    enabled: true
    # The observability data-read scopes, single-sourced: 59-am-mcp-client.yaml
    # subtracts this list from am-mcp's allowance and 73-am-obs-mcp-client.yaml
    # appends it to am-obs-mcp's OIDC scopes (both at template render time).
    # The same scopes also appear in ampScopes below as part of the full
    # catalog for the other interactive clients.
    observabilityScopes:
      - "amp:observability:trace-read"
      - "amp:observability:log-read"
      - "amp:observability:build-log-read"
      - "amp:observability:metric-read"
    # Full list of AMP OAuth2 scopes that interactive clients are allowed to request.
    # Thunder only grants scopes that appear in an app's registered scope list, so all
    # permission scopes must be declared here for role-based access to work.
    # Exception: am-mcp's allowance is this catalog MINUS observabilityScopes
    # (filtered in 59-am-mcp-client.yaml) — those scopes are owned exclusively
    # by the observer MCP client (amObsMcpClient below).
    ampScopes:
      - "openid"
      - "profile"
      - "email"
      - "amp:org:view"
      - "amp:org:modify-settings"
      - "amp:org:invite-member"
      - "amp:org:remove-member"
      - "amp:org:assign-role"
      - "amp:org:manage-idp"
      - "amp:org:manage-service-account"
      - "amp:project:create"
      - "amp:project:read"
      - "amp:project:update"
      - "amp:project:delete"
      - "amp:environment:create"
      - "amp:environment:read"
      - "amp:environment:update"
      - "amp:environment:delete"
      - "amp:gateway:create"
      - "amp:gateway:read"
      - "amp:gateway:update"
      - "amp:gateway:delete"
      - "amp:gateway:token-manage"
      - "amp:data-plane:read"
      - "amp:deployment-pipeline:create"
      - "amp:deployment-pipeline:read"
      - "amp:deployment-pipeline:update"
      - "amp:deployment-pipeline:delete"
      - "amp:git-secret:create"
      - "amp:git-secret:read"
      - "amp:git-secret:delete"
      - "amp:llm-provider-template:create"
      - "amp:llm-provider-template:read"
      - "amp:llm-provider-template:update"
      - "amp:llm-provider-template:delete"
      - "amp:llm-provider:create"
      - "amp:llm-provider:read"
      - "amp:llm-provider:update"
      - "amp:llm-provider:delete"
      - "amp:llm-provider:configure-guardrail"
      - "amp:llm-provider:connect"
      - "amp:llm-provider:deploy"
      - "amp:llm-provider:api-key-manage"
      - "amp:mcp-server:create"
      - "amp:mcp-server:read"
      - "amp:mcp-server:update"
      - "amp:mcp-server:delete"
      - "amp:mcp-server:configure-guardrail"
      - "amp:mcp-server:connect"
      - "amp:mcp-server:api-key-manage"
      - "amp:llm-proxy:create"
      - "amp:llm-proxy:read"
      - "amp:llm-proxy:update"
      - "amp:llm-proxy:delete"
      - "amp:llm-proxy:deploy"
      - "amp:llm-proxy:api-key-manage"
      - "amp:evaluator:create"
      - "amp:evaluator:read"
      - "amp:evaluator:update"
      - "amp:evaluator:delete"
      - "amp:agent:create"
      - "amp:agent:read"
      - "amp:agent:update"
      - "amp:agent:delete"
      - "amp:agent:build"
      - "amp:agent:deploy-non-production"
      - "amp:agent:deploy-production"
      - "amp:agent:promote"
      - "amp:agent:rollback"
      - "amp:agent:suspend"
      - "amp:agent:token-manage"
      - "amp:agent:api-key-manage"
      - "amp:agent-kind:read"
      - "amp:agent-kind:create"
      - "amp:agent-kind:update"
      - "amp:agent-kind:delete"
      - "amp:monitor:create"
      - "amp:monitor:read"
      - "amp:monitor:update"
      - "amp:monitor:delete"
      - "amp:monitor:execute"
      - "amp:monitor:score-read"
      - "amp:monitor:score-publish"
      - "amp:observability:trace-read"
      - "amp:observability:log-read"
      - "amp:observability:build-log-read"
      - "amp:observability:metric-read"
      - "amp:role:create"
      - "amp:role:read"
      - "amp:role:update"
      - "amp:role:delete"
      - "amp:group:create"
      - "amp:group:read"
      - "amp:group:update"
      - "amp:group:delete"
      - "amp:catalog:read"
      - "amp:repository:read"
      - "amp:profile:read"
      - "amp:profile:update-attributes"
      - "amp:scope:create"
      - "amp:scope:read"
      - "amp:scope:update"
      - "amp:scope:delete"
      - "amp:agent-identity:read"
      - "amp:agent-identity:create"
      - "amp:agent-identity:update"
      - "amp:agent-identity:delete"
    configMap:
      name: amp-thunder-bootstrap
      files:
        - 50-amp-api-client.yaml
        - 51-amp-console-theme.yaml
        - 52-amp-console-layout.yaml
        - 53-amp-console-app.yaml
        - 54-workload-publisher-app.yaml
        - 55-observer-resource-reader-app.yaml
        - 56-amp-publisher-client.yaml
        - 57-amp-system-client.yaml
        - 58-amctl-client.yaml
        - 59-am-mcp-client.yaml
        - 60-amp-resource-server.yaml
        - 61-amp-role-admin.yaml
        - 62-amp-role-developer.yaml
        - 63-amp-role-ai-lead.yaml
        - 64-amp-role-platform-engineer.yaml
        - 65-amp-group-admins.yaml
        - 66-amp-user-type-engineer.yaml
        - 67-amp-default-users.yaml
        - 68-amp-role-system-client-thunder-admin.yaml
        - 69-amp-default-resource-server-config.yaml
        - 70-fix-thunder-system-rs-identifier.yaml
        - 71-amp-cors-config.yaml
        - 72-am-observer-client.yaml
        - 73-am-obs-mcp-client.yaml
    # Observer Resource Reader Client configuration (for observability plane to read resources)
    observerResourceReaderClient:
      clientId: "openchoreo-observer-resource-reader-client" # clusterauthzrolebinding is already created with this clientId by openchoreo, so it should not be changed unless the corresponding ClusterAuthzRoleBinding is also updated
      clientSecret: "openchoreo-observer-resource-reader-client-secret"
      name: "OpenChoreo Observer Resource Reader"
      description: "OpenChoreo Observer Resource Reader Client for reading resources from control plane"
      grantTypes:
        - "client_credentials"
      tokenEndpointAuthMethod: "client_secret_post"
      pkceRequired: false
      publicClient: false
      accessTokenValidityPeriod: 3600
    # Workload Publisher Client configuration (for CI workflows to create workloads)
    workloadPublisherClient:
      clientId: "openchoreo-workload-publisher-client" # clusterauthzrolebinding is already created with this clientId by openchoreo, so it should not be changed unless the corresponding ClusterAuthzRoleBinding is also updated
      clientSecret: "openchoreo-workload-publisher-secret"
      name: "Workload Publisher"
      description: "OpenChoreo Workload Publisher Client for creating workloads from CI workflows"
      grantTypes:
        - "client_credentials"
      tokenEndpointAuthMethod: "client_secret_post"
      pkceRequired: false
      publicClient: false
      accessTokenValidityPeriod: 3600
    # AMP API Client configuration
    ampApiClient:
      clientId: "amp-api-client"
      clientSecret: "amp-api-client-secret"
      name: "AMP API Client"
      description: "Client application for AMP API access"
      grantTypes:
        - "client_credentials"
      tokenEndpointAuthMethod: "client_secret_basic"
      pkceRequired: false
      publicClient: false
      accessTokenValidityPeriod: 3600
    # AMP System Client configuration (for agent-manager to provision per-org OAuth apps via Thunder admin API)
    # WARNING: clientSecret below is a LOCAL-DEVELOPMENT-ONLY placeholder.
    # In production, override ampSystemClient.clientSecret via a Kubernetes Secret
    # or external secret manager. Deploying with this default value is insecure.
    ampSystemClient:
      clientId: "amp-system-client"
      clientSecret: "amp-system-client-secret"
      name: "AMP System Client"
      description: "System client for agent-manager to provision per-org OAuth apps"
      grantTypes:
        - "client_credentials"
      tokenEndpointAuthMethod: "client_secret_basic"
      pkceRequired: false
      publicClient: false
      accessTokenValidityPeriod: 3600
    # AMP Publisher Client configuration (for evaluation job to publish scores)
    ampPublisherClient:
      clientId: "amp-publisher-client"
      clientSecret: "amp-publisher-client-secret"  # Override in production; must match the value stored in OpenBao at secret/amp-publisher-client-secret (used by ExternalSecret at workflow runtime)
      name: "AMP Publisher Client"
      description: "Client application for evaluation job to publish scores"
      grantTypes:
        - "client_credentials"
      tokenEndpointAuthMethod: "client_secret_basic"
      pkceRequired: false
      publicClient: false
      accessTokenValidityPeriod: 3600
    # AM Observer Client configuration (dedicated outbound identity for
    # agent-manager-observer's calls to the OpenChoreo observer backend).
    # Its authority comes from the OpenChoreo ClusterAuthzRoleBinding
    # (amp-observer-reader-binding in wso2-amp-platform-resources-extension),
    # NOT from AMP scopes — it has no scope allowance at all, so its tokens
    # fail agent-manager-service's scope checks by construction.
    # WARNING: clientSecret below is a LOCAL-DEVELOPMENT-ONLY placeholder.
    # In production, override amObserverClient.clientSecret via a Kubernetes
    # Secret or external secret manager. Deploying with this default value is
    # insecure.
    # NOTE: pre-install bootstrap job only — see the upgrade note on
    # mcpResourceServers below / docs/reference/mcp-server.mdx.
    amObserverClient:
      clientId: "am-observer-client"
      clientSecret: "am-observer-client-secret"
      name: "AM Observer Client"
      description: "Dedicated client for agent-manager-observer to read observability data from the OpenChoreo observer"
      grantTypes:
        - "client_credentials"
      tokenEndpointAuthMethod: "client_secret_basic"
      pkceRequired: false
      publicClient: false
      accessTokenValidityPeriod: 3600
    # AMP CLI Client configuration (public PKCE client for interactive login)
    amCliClient:
      clientId: "amctl"
      name: "AMCTL"
      description: "AMP CLI application for interactive user login"
      redirectUris:
        - "http://127.0.0.1:10325/callback"
      grantTypes:
        - "authorization_code"
        - "refresh_token"
      responseTypes:
        - "code"
      allowedUserTypes:
        - "engineer"
      tokenEndpointAuthMethod: "none"
      pkceRequired: true
      publicClient: true
      accessTokenValidityPeriod: 3600
      idTokenValidityPeriod: 3600
      userAttributes:
        - "given_name"
        - "family_name"
        - "email"
        - "username"
        - "groups"
        - "ouId"
        - "ouHandle"
    # AMP MCP Client configuration (public PKCE client for interactive login)
    amMcpClient:
      clientId: "am-mcp"
      name: "AMP MCP"
      description: "AMP MCP application for interactive user login"
      redirectUris:
        - "http://127.0.0.1:33418/callback"
        - "http://localhost:33418/callback"
      grantTypes:
        - "authorization_code"
        - "refresh_token"
      responseTypes:
        - "code"
      allowedUserTypes:
        - "engineer"
      tokenEndpointAuthMethod: "none"
      pkceRequired: true
      publicClient: true
      accessTokenValidityPeriod: 3600
      idTokenValidityPeriod: 3600
      userAttributes:
        - "given_name"
        - "family_name"
        - "email"
        - "username"
        - "groups"
        - "ouId"
        - "ouHandle"
    # AMP Observer MCP Client configuration (public PKCE client for the
    # observer MCP server's interactive login). Allowed ONLY the OIDC scopes
    # plus the four observability data-read scopes; the am-service MCP client
    # (amMcpClient above) is allowed the rest of the catalog.
    # NOTE: pre-install bootstrap job only — see the upgrade note on
    # mcpResourceServers below / docs/reference/mcp-server.mdx.
    amObsMcpClient:
      clientId: "am-obs-mcp"
      name: "AMP Observer MCP"
      description: "AMP Observer MCP application for interactive user login"
      redirectUris:
        - "http://127.0.0.1:33419/callback"
        - "http://localhost:33419/callback"
      grantTypes:
        - "authorization_code"
        - "refresh_token"
      responseTypes:
        - "code"
      allowedUserTypes:
        - "engineer"
      tokenEndpointAuthMethod: "none"
      pkceRequired: true
      publicClient: true
      accessTokenValidityPeriod: 3600
      idTokenValidityPeriod: 3600
      # OIDC scopes only — the four observability data-read scopes are
      # appended from observabilityScopes at template render time
      # (73-am-obs-mcp-client.yaml).
      scopes:
        - "openid"
        - "profile"
        - "email"
      userAttributes:
        - "given_name"
        - "family_name"
        - "email"
        - "username"
        - "groups"
        - "ouId"
        - "ouHandle"
    # MCP resource servers — RFC 8707 resource indicators for the platform's
    # MCP endpoints. Thunder (v0.45+) rejects an authorize request with
    # invalid_target unless its `resource` parameter exactly matches a
    # registered resource-server identifier. MCP clients send the canonical
    # origin of the MCP endpoint WITH a trailing slash, so each identifier
    # must be the externally reachable base URL of the service (the same value
    # as agent-manager's serverPublicURL / the observability extension's
    # amObserver.publicUrl), slash included.
    #
    # Thunder additionally downscopes granted scopes to the permissions defined
    # on the resource server named in `resource`, so each entry mirrors the
    # slice of the amp permission tree its MCP client is allowed, selected by
    # permissionSet:
    #   - "amp-minus-observability": the full amp tree minus the four
    #     amp:observability:*-read actions (matches the am-mcp client)
    #   - "observability-only": only the amp:observability resource with its
    #     four *-read actions (matches the am-obs-mcp client)
    # The handle MUST stay empty: Thunder prefixes derived permissions with the
    # resource-server handle, and the scope strings must come out identical to
    # the amp resource server's (amp:project:read etc.). The bootstrap roots
    # the tree under a top-level "amp" resource instead.
    # NOTE: seeded by the pre-install bootstrap job only — on existing installs,
    # re-run the bootstrap job (it automatically replaces MCP resource servers
    # created by an older bootstrap with a non-empty handle); a helm upgrade
    # alone will not re-seed.
    #
    # Each resource server's identifier is derived by the bootstrap template from
    # the base URL named in baseUrlValue, with the trailing slash appended. The
    # base URLs live as their own scalars below rather than inline here so a
    # deployment can override just the host: `helm --set` merges a scalar, but
    # `--set mcpResourceServers[0].identifier=...` REPLACES the whole list element
    # and silently drops name/handle/description/permissionSet. Point each base
    # URL at the externally reachable origin of its MCP endpoint's service — the
    # same value as agent-manager's serverPublicURL and the observability
    # extension's amObserver.publicUrl. Defaults suit a k3d/local install, where
    # amp-api is reached through the OpenChoreo control-plane gateway rather than
    # on its container port.
    #
    # An identifier is matched EXACTLY, so a service reachable on more than one
    # origin needs one entry per origin. Entries marked `optional: true` are
    # skipped when their base URL is empty. agentManagerMcpDevBaseUrl is the
    # opt-in second origin for the dev stack, where `make setup`
    # (deployments/setup/setup-openchoreo.sh, this chart's only installer on that
    # path) runs agent-manager-service as a host process on :9000 — an MCP client
    # there asks for resource http://localhost:9000/, so without that entry
    # Thunder answers invalid_target. setup-openchoreo.sh sets it.
    agentManagerMcpBaseUrl: "http://api.amp.localhost:8080"
    agentManagerMcpDevBaseUrl: ""
    observerMcpBaseUrl: "http://traces.amp.localhost:11080"
    mcpResourceServers:
      - name: "AMP Agent Manager MCP"
        handle: ""
        baseUrlValue: "agentManagerMcpBaseUrl"
        description: "Resource identifier for the agent-manager MCP endpoint"
        permissionSet: "amp-minus-observability"
      - name: "AMP Agent Manager MCP (dev)"
        handle: ""
        baseUrlValue: "agentManagerMcpDevBaseUrl"
        optional: true
        description: "Resource identifier for the agent-manager MCP endpoint on the docker-compose dev stack"
        permissionSet: "amp-minus-observability"
      - name: "AMP Observer MCP"
        handle: ""
        baseUrlValue: "observerMcpBaseUrl"
        description: "Resource identifier for the observer MCP endpoint"
        permissionSet: "observability-only"
    # AMP Console Client configuration
    ampConsoleClient:
      clientId: "amp-console-client"
      clientSecret: "amp-console-client-secret"
      name: "AMP Console"
      description: "AMP Console Application"
      logoUrl: "https://wso2.cachefly.net/wso2/sites/all/image_resources/logos/WSO2-Pulse-Orange.webp"
      redirectUris:
        - "http://localhost:3000/login"
        - "http://console.amp.localhost:8080/login"
      grantTypes:
        - "authorization_code"
        - "refresh_token"
      responseTypes:
        - "code"
      allowedUserTypes:
        - "engineer"
      tokenEndpointAuthMethod: "none"
      pkceRequired: true
      publicClient: true
      accessTokenValidityPeriod: 3600
      idTokenValidityPeriod: 3600
      userAttributes:
        - "given_name"
        - "family_name"
        - "email"
        - "username"
        - "groups"
        - "ouId"
        - "ouHandle"

    # Agent Manager Console Theme (applied to amp-console-client sign-in page)
    ampConsoleTheme:
      handle: "wso2-agent-manager-console"
      displayName: "WSO2 Agent Manager Console Theme"
      theme:
        direction: "ltr"
        defaultColorScheme: "light"
        shape:
          borderRadius: 8
        typography:
          fontFamily: "Inter Variable, sans-serif"
          h1:
            fontSize: "1.75rem"
            fontWeight: 600
            lineHeight: 1.3
        colorSchemes:
          light:
            colors:
              primary:
                main: "#fa7b3f"
                dark: "#e74420"
                contrastText: "#ffffff"
              secondary:
                main: "#1a1a2e"
                dark: "#0d0d1a"
                contrastText: "#ffffff"
              background:
                default: "#f7f8fb"
                paper: "#ffffff"
              text:
                primary: "#000000"
                secondary: "#494848"
          dark:
            colors:
              primary:
                main: "#fa7b3f"
                dark: "#e74420"
                contrastText: "#ffffff"
              secondary:
                main: "#ff9966"
                dark: "#fa7b3f"
                contrastText: "#000000"
              background:
                default: "#121212"
                paper: "#1e1e1e"
              text:
                primary: "#ffffff"
                secondary: "#cccccc"
    # Agent Manager Console Layout (split-screen sign-in page for amp-console-client).
    ampConsoleLayout:
      id: "0e0dc039-bae9-4563-82b9-ee4e76f9a58c"
      handle: "wso2-agent-manager-console"
      displayName: "WSO2 Agent Manager Console Layout"
      layout:
        head:
          stylesheets:
            - id: "wso2-agent-manager-console-overrides"
              type: "inline"
              content: |
                body { background: #fafafa !important; min-height: 100vh !important; margin: 0 !important; }
                [data-color-scheme="dark"] body { background: #0b0b0f !important; }
                body { display: grid !important; grid-template-columns: 1fr !important; align-items: stretch !important; }
                body::before { display: none; }
                @media (min-width: 900px) {
                  body { grid-template-columns: minmax(380px, 1fr) minmax(420px, 1fr) !important; }
                  body::before { content: ""; display: block; background-color: #0a0a0e; background-image: radial-gradient(800px 400px at 0% 0%, rgba(250,123,63,0.18), transparent 60%), url("https://wso2.cachefly.net/wso2/sites/all/image_resources/wso2-agent-manager-auth-marketing-dark.svg"); background-position: top left, center center; background-repeat: no-repeat, no-repeat; background-size: auto, min(540px, calc(100% - 96px)) auto; }
                  [data-color-scheme="light"] body::before { background-color: #f3f4f6; background-image: radial-gradient(800px 400px at 0% 0%, rgba(250,123,63,0.10), transparent 60%), url("https://wso2.cachefly.net/wso2/sites/all/image_resources/wso2-agent-manager-auth-marketing-light.svg"); }
                }
                .MuiPaper-root { max-width: 420px !important; width: 100% !important; margin: auto !important; padding: 40px 36px !important; border-radius: 16px !important; background-color: #ffffff !important; box-shadow: 0 1px 2px rgba(15,23,42,0.04), 0 12px 40px rgba(15,23,42,0.08) !important; border: 1px solid rgba(15,23,42,0.06) !important; }
                [data-color-scheme="dark"] .MuiPaper-root { background-color: #14141a !important; box-shadow: 0 1px 2px rgba(0,0,0,0.4), 0 16px 48px rgba(0,0,0,0.55) !important; border-color: rgba(255,255,255,0.08) !important; }
                img[alt*="logo" i], img[src*="logo" i] { display: block !important; max-height: 56px !important; width: auto !important; margin: 0 auto 28px !important; }
                .Text--h1, .MuiTypography-h1, h1.MuiTypography-root { display: block !important; text-align: center !important; font-size: 1.5rem !important; line-height: 1.3 !important; font-weight: 600 !important; letter-spacing: -0.01em !important; color: #111827 !important; margin: 0 0 4px !important; }
                [data-color-scheme="dark"] .Text--h1, [data-color-scheme="dark"] .MuiTypography-h1, [data-color-scheme="dark"] h1.MuiTypography-root { color: #f5f5f7 !important; }
                .Text--body1, .MuiTypography-body1 { text-align: center !important; font-size: 0.9375rem !important; color: rgba(15,23,42,0.6) !important; margin: 0 0 8px !important; line-height: 1.5 !important; }
                [data-color-scheme="dark"] .Text--body1, [data-color-scheme="dark"] .MuiTypography-body1 { color: rgba(255,255,255,0.6) !important; }
                .MuiPaper-root .ThunderFormControl--root { margin-top: 0 !important; margin-bottom: 0 !important; }
                .MuiPaper-root .ThunderLabel--root { font-size: 0.8125rem !important; font-weight: 500 !important; margin-bottom: 6px !important; color: rgba(15,23,42,0.7) !important; }
                [data-color-scheme="dark"] .MuiPaper-root .ThunderLabel--root { color: rgba(255,255,255,0.7) !important; }
                .MuiPaper-root .ThunderTextField--root .MuiOutlinedInput-root { border-radius: 10px !important; font-size: 0.9375rem !important; }
                .MuiPaper-root .ThunderTextField--root .MuiOutlinedInput-input { padding: 11px 14px !important; height: auto !important; }
                .MuiPaper-root .ThunderIDFlow--richText:not(:last-child) { display: none !important; margin: 4px 0 8px !important; padding: 8px 10px !important; font-size: 0.75rem !important; line-height: 1.45 !important; color: rgba(15,23,42,0.55) !important; text-align: center !important; background: rgba(15,23,42,0.04) !important; border-radius: 8px !important; }
                [data-color-scheme="dark"] .MuiPaper-root .ThunderIDFlow--richText:not(:last-child) { color: rgba(255,255,255,0.55) !important; background: rgba(255,255,255,0.04) !important; }
                .MuiPaper-root .MuiStack-root, .MuiPaper-root [class*="BlockStack"], .MuiPaper-root form { gap: 12px !important; margin-top: 28px !important; }
                .MuiButton-root { width: 100% !important; border-radius: 10px !important; text-transform: none !important; font-weight: 500 !important; font-size: 0.9375rem !important; padding: 11px 16px !important; height: 44px !important; justify-content: center !important; gap: 10px !important; box-shadow: none !important; transition: transform 120ms ease, box-shadow 160ms ease, background-color 160ms ease, border-color 160ms ease !important; }
                .MuiPaper-root .ThunderFlow--triggerButton:nth-of-type(1)::before { content: ""; display: inline-block; width: 18px; height: 18px; background: center/contain no-repeat url("https://www.gstatic.com/images/branding/googleg/1x/googleg_standard_color_128dp.png"); flex: 0 0 18px; }
                .MuiPaper-root .ThunderFlow--triggerButton:nth-of-type(2)::before { content: ""; display: inline-block; width: 18px; height: 18px; background: center/contain no-repeat url("https://upload.wikimedia.org/wikipedia/commons/9/91/Octicons-mark-github.svg"); flex: 0 0 18px; }
                .MuiPaper-root .MuiButton-root { background-color: #ffffff !important; color: rgba(0,0,0,0.87) !important; border: 1px solid rgba(0,0,0,0.12) !important; box-shadow: 0 1px 2px rgba(0,0,0,0.08) !important; }
                .MuiPaper-root .MuiButton-root:hover { background-color: #f8f9fa !important; box-shadow: 0 2px 6px rgba(0,0,0,0.12) !important; transform: translateY(-1px) !important; }
                [data-color-scheme="dark"] .MuiPaper-root .MuiButton-root { border-color: rgba(255,255,255,0.18) !important; }
                [data-color-scheme="dark"] .MuiPaper-root .MuiButton-root:hover { background-color: #f1f3f4 !important; }
                .MuiButton-outlined, .MuiButton-contained { box-shadow: inherit !important; }
                .MuiPaper-root .ThunderIDFlow--richText:last-child { display: none !important; margin-top: 28px !important; padding-top: 20px !important; border-top: 1px solid rgba(15,23,42,0.06) !important; font-size: 0.75rem !important; line-height: 1.5 !important; color: rgba(15,23,42,0.5) !important; text-align: center !important; margin-bottom: 0 !important; }
                .MuiPaper-root .ThunderIDFlow--richText:last-child a { color: rgba(15,23,42,0.7) !important; text-decoration: underline !important; text-underline-offset: 2px !important; font-size: inherit !important; }
                .MuiPaper-root .ThunderIDFlow--richText:last-child a:hover { color: #fa7b3f !important; }
                [data-color-scheme="dark"] .MuiPaper-root .ThunderIDFlow--richText:last-child { border-top-color: rgba(255,255,255,0.08) !important; color: rgba(255,255,255,0.45) !important; }
                [data-color-scheme="dark"] .MuiPaper-root .ThunderIDFlow--richText:last-child a { color: rgba(255,255,255,0.65) !important; }
                [data-color-scheme="dark"] .MuiPaper-root .ThunderIDFlow--richText:last-child a:hover { color: #fa7b3f !important; }
        screens:
          signin:
            title: "Sign In"
            layout: "split"
          signup:
            title: "Create your WSO2 Cloud account"
            layout: "split"
          common:
            layout: "split"
    # Default Organization unit configuration
    organizationUnit:
      name: "Default"
      handle: "default"
      description: "Default organizational unit"
    # User schema configuration
    userSchema:
      name: "engineer"
      allowSelfRegistration: true
    # Default users configuration
    defaultUsers:
      - username: "admin"
        password: "admin"
        givenName: "Admin"
        familyName: "User"
        groups: []